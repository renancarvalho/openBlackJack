define(['jquery','backbone', 'hb!../view/template/menu.tpl'],
	function($, Backbone, Template) {
		var MenuView = Backbone.View.extend({

		    initialize : function(options){
		    	
		    }

		});
		return MenuView;
	}
);
